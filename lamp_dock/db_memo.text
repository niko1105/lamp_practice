orders
    order_id
    user_id
    created
    updated

order_details
    order_detail_id
    order_id
    item_id
    price
    amount
    created
    updated


SELECT
			item_id,
         SUM(amount) as total_amount
FROM
			order_details
GROUP BY
			item_id
ORDER BY
			total_amount DESC
LIMIT
		0,3
			